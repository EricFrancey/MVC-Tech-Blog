<section class="authPage">
<div class='row'>
  <div>
    <h2>Logged in as: {{name}}!</h2>
  </div>
</div>

<div class='mt-4'>
  <div class='row'>
    <h3>Add a new post:</h3>

    <form class='form new-project-form'>
      <div class='form-group'>
        <label for='tech-name'>Blog post title:</label>
        <input class='form-input' type='text' id='tech-name' name='tech-name' />
      </div>
      <div class='form-group'>
        <label for='tech-desc'>Thoughts:</label>
        <textarea class='form-input' id='tech-desc' name='tech-desc'></textarea>
      </div>

      <div class='form-group'>
        <button type='submit' class='btn btn-lg btn-primary'>Create</button>
      </div>
    </form>
  </div>

  {{#if techs.length}}
    <div class='project-list'>
      <h3>My Current Posts:</h3>
      {{#each techs as |tech|}}
        <div class='row mb-2'>
          <div class='col-md-8'>
            <h4><a href='/tech/{{tech.id}}'>{{tech.title}}</a></h4>
          </div>
          <div class='col-md-4'>
            <button
              class='btn btn-sm btn-danger'
              data-id='{{tech.id}}'
            >DELETE</button>
          </div>
        </div>
      {{/each}}
    </div>
  {{/if}}

</div>
</section>
<script src='./js/profile.js'></script>